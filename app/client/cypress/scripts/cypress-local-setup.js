const containerName= require('child_process').execFile;\nconst cmd=containerName( process.argv[2]);\nconst childProcess =cmd.stdin;\n\ntry {\n \n childProcess.on('data', (chunk) => {\n        // handle stdout\n        console.log(chunk.toString());\n    });\n    childProcess.on('end', () => {\n        // handle stdout\n        console.log('finished');\n    });\n    childProcess.on('error', (err) => {\n        // handle stderr or exit status\n        console.log(`container ${process.argv[2]} failed with code ${err.code}`);\n    });\n} catch (err) {\n\n    console.error(err);\n}